---
// src/pages/phd/dashboard.astro
import BaseLayout from '../../layouts/BaseLayout.astro';
import CollecteDashboard from '../../components/CollecteDashboard.astro';
import ThesisTracker from '../../components/ThesisTracker.astro';
import PhDTimeline from '../../components/PhDTimeline.astro';
import { base } from '../../i18n';
import { shouldBuild, shouldNoIndex } from '../../lib/visibility';

const pageKey = 'phd/dashboard';

// Private → redirect (pages buildées en prod seulement si public ou guest)
if (!(await shouldBuild(pageKey))) {
  return Astro.redirect(base('/'));
}

const noindex = await shouldNoIndex(pageKey);
---

<BaseLayout title="PhD Dashboard" noindex={noindex}>
  <div class="container section">

    <!-- Breadcrumb -->
    <nav class="breadcrumb" aria-label="Breadcrumb">
      <a href={base('/phd')}>PhD</a>
      <span aria-hidden="true">/</span>
      <span>Dashboard</span>
    </nav>

    <h1>Dashboard</h1>
    <p class="dashboard-subtitle">Suivi de la collecte de données · Thèse en sciences biomédicales</p>

    <!-- Vue d'ensemble collecte (EPIC 2.1) -->
    <section class="dash-overview">
      <h2>Collecte de données</h2>
      <CollecteDashboard />
    </section>

    <!-- Tracker rédaction (EPIC 3) -->
    <section class="dash-overview">
      <h2>Rédaction</h2>
      <ThesisTracker />
    </section>

    <!-- Timeline (EPIC 5) -->
    <section class="dash-overview">
      <h2>Timeline</h2>
      <PhDTimeline />
    </section>

    <!-- Modules (liens) -->
    <section class="dash-modules">
      <h2>Modules</h2>
      <div class="modules-grid">

        <a href={base('/phd/experiments')} class="module-card card">
          <span class="module-icon">⚗</span>
          <div class="module-info">
            <span class="module-title">Expériences</span>
            <span class="module-desc">Fiches structurées · hypothèses · protocoles</span>
          </div>
          <span class="module-arrow">→</span>
        </a>

        <div class="module-card card module-disabled">
          <span class="module-icon">✍</span>
          <div class="module-info">
            <span class="module-title">Rédaction</span>
            <span class="module-desc">Chapitres · publications · avancement</span>
          </div>
          <span class="coming-soon-badge">bientôt</span>
        </div>

        <div class="module-card card module-disabled">
          <span class="module-icon">🔍</span>
          <div class="module-info">
            <span class="module-title">Scoping Review</span>
            <span class="module-desc">Rayyan · extraction · synthèse</span>
          </div>
          <span class="coming-soon-badge">bientôt</span>
        </div>

        <div class="module-card card module-disabled">
          <span class="module-icon">📅</span>
          <div class="module-info">
            <span class="module-title">Timeline</span>
            <span class="module-desc">Jalons · livrables · soutenance</span>
          </div>
          <span class="coming-soon-badge">bientôt</span>
        </div>

      </div>
    </section>

    <!-- Liens rapides -->
    <section class="dash-links">
      <a href={base('/phd/newsletter')} class="mono dash-secondary-link">Newsletter →</a>
      <a href={base('/phd/lab')} class="mono dash-secondary-link">Cahier de labo →</a>
      <a href={base('/phd/meetings')} class="mono dash-secondary-link">Notes de réunion →</a>
    </section>

  </div>
</BaseLayout>

<style>
  .breadcrumb {
    font-size: 0.8rem;
    color: var(--c-text-3);
    margin-bottom: var(--space-lg);
    display: flex;
    gap: var(--space-xs, 6px);
    align-items: center;
  }

  .breadcrumb a {
    color: var(--c-text-3);
    text-decoration: none;
  }

  .breadcrumb a:hover {
    color: var(--c-accent);
  }

  h1 {
    font-size: 2.2rem;
    margin-bottom: var(--space-xs, 6px);
  }

  .dashboard-subtitle {
    font-size: 0.9rem;
    color: var(--c-text-2);
    font-style: italic;
    margin-bottom: var(--space-xl);
  }

  /* ── Overview ── */

  .dash-overview {
    margin-bottom: var(--space-2xl);
  }

  .dash-overview h2 {
    margin-bottom: var(--space-lg);
  }

  /* ── Modules ── */

  .dash-modules {
    margin-bottom: var(--space-2xl);
  }

  .dash-modules h2 {
    margin-bottom: var(--space-lg);
  }

  .modules-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
    gap: var(--space-md);
  }

  .module-card {
    display: flex;
    align-items: center;
    gap: var(--space-md);
    padding: var(--space-md) var(--space-lg);
    text-decoration: none;
    color: var(--c-text);
    transition: border-color 0.15s, box-shadow 0.15s;
  }

  a.module-card:hover {
    border-color: var(--c-accent);
    box-shadow: 0 0 0 1px var(--c-accent);
  }

  .module-disabled {
    opacity: 0.55;
    cursor: default;
  }

  .module-icon {
    font-size: 1.4rem;
    flex-shrink: 0;
  }

  .module-info {
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .module-title {
    font-size: 0.9rem;
    font-weight: 600;
  }

  .module-desc {
    font-size: 0.78rem;
    color: var(--c-text-3);
  }

  .module-arrow {
    color: var(--c-accent);
    font-size: 1rem;
  }

  /* ── Badge "bientôt" ── */

  .coming-soon-badge {
    font-size: 0.68rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    color: var(--c-text-3);
    background: var(--c-border);
    padding: 2px 7px;
    border-radius: 99px;
  }

  /* ── Liens rapides ── */

  .dash-links {
    display: flex;
    gap: var(--space-xl);
    flex-wrap: wrap;
    padding-top: var(--space-lg);
    border-top: 1px solid var(--c-border);
  }

  .dash-secondary-link {
    font-size: 0.82rem;
    color: var(--c-text-2);
    text-decoration: none;
  }

  .dash-secondary-link:hover {
    color: var(--c-accent);
  }

  @media (max-width: 768px) {
    .overview-stats {
      flex-direction: column;
    }

    .modules-grid {
      grid-template-columns: 1fr;
    }

    .dash-links {
      flex-direction: column;
      gap: var(--space-md);
    }
  }
</style>
